// Milestone 2a transport spike: a stable, unhashed static module served by Vite
// at a known path, used as the bare-specifier import-map target.
export const spikeMessage = "spike-dep resolved via import map";
